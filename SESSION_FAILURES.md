- **Root gate wrapper import**: Ran `scripts/gate.py` through `runpy` without adding the bundled plugin tool directory to `sys.path`. It failed with `ModuleNotFoundError: not_ai_core`. Do not use `runpy` for this wrapper without inserting `plugins/not-ai/tools` first.
- **Canonical skill replacement**: Tried to delete and re-add `plugins/not-ai/skills/not-ai/SKILL.md` in one `apply_patch` call. The patch engine rejects multiple operations on one path. Use separate delete and add patches.
- **Benchmark fixture patch**: Tried to add new fixture files without prefixing content lines with `+`. `apply_patch` rejected the hunk. Prefix every line in an `Add File` block.
- **Analyzer parity wording**: Updated the structural analyzer's contextual vocabulary note without making the identical update in `measure.py`; the parity suite reported 41 mismatches. Keep shared output fields byte-for-byte aligned and run `verify_measure.py` after either implementation changes.
- **Parity fix patch context**: The first fix assumed `measure.py` formatted the return mapping across separate key lines, so `apply_patch` could not find the context. Read the exact local formatting before patching mirrored implementations.
- **Prose scan specimen return type**: `scan()` returned a two-list tuple for exempt `input.md` files while callers expected one list. Repository aggregation silently added twelve list objects, then crashed while printing them. Keep every branch of a collection-returning function on the same type and cover exemptions directly.
- **Skill validator runtime**: The system `quick_validate.py` could not start under the default Python because PyYAML is unavailable there. Use the bundled workspace Python when present; do not install a project dependency just to run an external validator.
- **Bundled validator runtime**: The bundled workspace Python also lacks PyYAML, so it cannot run the system validator either. When both environments lack the validator's sole dependency, reproduce its documented read-only checks with an available YAML parser and report the validator itself as unavailable.
